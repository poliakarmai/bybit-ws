"""bybit_ws.experiments — воспроизводимое дерево экспериментов.

Пакет для выноса поиска эджа с live-аккаунта на изолированную песочницу:
  - experiment / run — immutable-модель дерева экспериментов (store.py)
  - engine.py       — детерминированный backtest-движок (обёртка над paper_trade)
  - backtest_runner — одиночный воспроизводимый прогон + run-манифест
  - parallel_sandbox— параллельный прогон N вариантов на одном диапазоне истории

Ничего здесь не пишет в боевой ~/.local/share/bybit-ws/state.db — только отдельная
experiments-БД и кэш исторических свечей в собственном каталоге.
"""

__version__ = "0.1.0"
